using CentreT_TelegramBot.Repositories.Infrastructure;
using Microsoft.EntityFrameworkCore;

namespace CentreT_TelegramBot.Repositories;

public class UserJoinContextRepository : GenericRepository<Entities.UserJoinContext>, IUserJoinContextRepository
{
    public UserJoinContextRepository(BotDbContext dbContext) : base(dbContext)
    {
    }
}